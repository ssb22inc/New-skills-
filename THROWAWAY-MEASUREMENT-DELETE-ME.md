Throwaway. Created to measure whether this repository blocks a merge when no
gate reports. Path is deliberately outside `fullburn/**` and `.github/**` so
that `fullburn-ci`'s paths filter produces no run at all — the exact
"check never reports" condition. Delete this branch and PR.
