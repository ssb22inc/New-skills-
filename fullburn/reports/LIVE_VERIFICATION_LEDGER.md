# LIVE VERIFICATION LEDGER (adversary finding R7)

Machine-readable list of acceptance criteria whose LIVE halves cannot be
verified in the build sandbox. Each entry: what is unmet → which human task
unblocks it → the exact verification to run once unblocked. An entry leaves
this ledger only when its verification has been executed and recorded by the
adversary. **While any entry is open, every phase verdict is CONDITIONAL, and
Phase 1 implementation cannot be adversary-passed (its brief generator needs a
live LLM path).**

| # | Blocked verification | Phase/AC | Blocking task | Verification when unblocked |
|---|---|---|---|---|
| L1 | Hello-world call round-trips through the REAL Cloudflare AI Gateway and the trace appears in the REAL Langfuse project | Phase 0 AC 1 (live half) | H2, H5, H6, H7 | `llm()` with role `hello-world` against production `gatewayBaseUrl` + Langfuse sink; adversary independently confirms the trace in the Langfuse UI |
| L2 | Genome-tagger golden-set outputs regenerated from LIVE models (current recorded outputs are authored placeholders that exercise the harness only) | Phase 0 AC 2 (live half) | H6 | Re-run `runEval` with fresh transport recordings for each candidate; commit recordings; re-run rebind test |
| L3 | Eval results pushed to Langfuse (eval harness ↔ Langfuse adapter live) | Phase 0 deliverable (Langfuse eval harness, live half) | H5 | Harness pushes an eval run; adversary confirms it in Langfuse |
| L4 | **PROMOTED 2026-08-21 TO THE PRIMARY AUTHORITATIVE SPEND CONTROL** (human ruling on R14-01). It was recorded here as "defense-in-depth" behind a local check; R14-01 measured that the local check cannot bound its own process, so the roles are reversed: the Gateway-side per-client cap is the authority and `spend-ledger.ts` is advisory fast-refuse. See L31 for the split and the executed proof. **This makes H2 a Phase 0 blocker**: until these caps are configured Gateway-side and verified against `caps.ts`, the primary control is designed and unprovisioned, and nothing in-process can stand in for it | Phase 0 (§2.2) — **blocking** | H2 | Configure Gateway caps; adversary attempts an over-cap call with local check bypassed in a test harness — Gateway must refuse |
| L5 | CI runs on GitHub Actions with secrets in repo settings; leak-check proven against real secret material | Phase 0 (§10.3) | H7 | First real PR exercises all three gates on github.com |
| L6 | fullburn.ai registered; formal trademark check completed | Phase 0 deliverable | H1 | Registrar + trademark confirmation recorded here |
| L7 | Workers-runtime test pool (workerd) in CI — engine src is written platform-neutral against the Workers target (wrangler.toml committed), but tests currently execute on Node | Phase 0 (§2.2, R12 partial) | none (buildable; queued behind Phase A findings) | Add @cloudflare/vitest-pool-workers, run suite under workerd, record parity |
| L8 | Cross-family adversary re-review: this phase's build adversary ran on the SAME model family as the builder (harness limitation) — §2.4 family diversity violated for the review itself | Phase 0 process (R9b) | H6b | Re-run the Phase 0 adversary review on a non-Claude model; record deltas as findings |
| L9 | **ClickHouse Cloud instance + Airbyte instance provisioned** — named Phase 0 deliverables with no code, no connection and no schema in the repo. Phase 0 could otherwise be signed off with no warehouse and no ingestion (adversary finding F10) | Phase 0 deliverable | H3, H4 | Provision both; connect a Worker to ClickHouse over HTTPS and land one row; adversary confirms the row and the per-client schema isolation |
| L10 | **OAuth vault is least-scope but neither encrypted nor auto-rotated** — `MemoryVaultBackend` is in-memory plaintext with a manual `rotate()`. §15 requires vault-encrypted, auto-rotated secrets with a drilled breach runbook (adversary finding F10) | Phase 0 deliverable (§15) | H7 | Back the vault with encrypted storage + automatic rotation; run the breach runbook drill (revoke → rotate → notify → audit) and record time-to-rotate |
| L11 | **Repository protection is not in place** — required status checks, no-force-push, and CODEOWNERS on `APPROVALS/**` + every Class-2 path. Until then the CI gates are advisory and an approval entry proves *what* was approved, never *who* approved it (adversary finding F14) | Phase 0 (§10.3) | H19 | Enable protection on GitHub; adversary attempts a merge with a red check and a self-authored approval — both must be refused |
| L12 | **Eval-attestation provenance has an in-process ceiling** — `attestEvalRun` requires a full run over the role's declared case ids and mints an unforgeable branded object, so a literal no longer binds (R2-22). But any module that can import the factory can call it: this is not cryptographic provenance, and a compromised in-repo module could still mint one | Phase 0 (§2.4, Law 13) | none (design limit) | When Langfuse eval push is live (L3), require the attestation to carry a Langfuse run id the registry can independently confirm; adversary attempts to bind without a corresponding remote run |
| L13 | **The Class-2 gate proves what was approved, never who approved it** — approvals are now transition-bound (from-hash → to-hash) so a superseded approval cannot be replayed (R2-05), but any actor with write access can still author one | Phase 0 (§13) | H19 | CODEOWNERS on `APPROVALS/**`; adversary attempts a self-authored approval on a PR and confirms review is required |

| L14 | **The AI cap rolls over by UTC day and does not survive a restart** — ledgers are now keyed by (client, UTC day), so a spent client is no longer locked out forever (M-03). **CORRECTED 2026-08-19 (adversary finding R12-05): BOTH GAPS THIS ROW DESCRIBED ARE CLOSED, and the row spent five rounds describing the cap as broken in ways it is not.** Rollover has been client-local since R7-02 — `zoneDayKey(nowMs, caps.ianaTimeZone)`, with the zone travelling with the ceilings (measured: pulsern's ledger day is `America/New_York`, one day behind UTC at 01:00Z). And a fresh meter no longer starts a fresh day: since R11-07 the meter is a handle onto one process-wide ledger, and since R12-01 that ledger owns the arithmetic. WHAT REMAINS is narrower and is the whole of this row now: the ledger is per PROCESS, so two Workers hold two ceilings, and nothing survives a restart. Bound by the ledger-claims check in `engine/test/invariants/` | Phase 0 (Law 2, §2.2) | H2 (Durable Object storage), plus the market registry's locale clock | Back the meter with DO storage keyed by client-local day; adversary restarts the DO mid-day and confirms the ceiling does NOT reset, and that rollover happens on the client's midnight |
| L15 | **~30 r3 findings remain open and queued** — the r3 review confirmed 47; this pass fixed the 7 money-loss and control-plane findings and locked 14 fixes with mutation-verified tests. The remaining data-lie, isolation and dummy-proof findings (including R2-13's registry half, R2-23's golden-set assertions, R2-29's sibling-tree scan scope, and the §10.2 self-check's identity gap) are recorded in reports/ADVERSARY_REPORT_phase0.r3.md and NOT yet fixed | Phase 0 | none (queued by human decision) | Fix and re-attack in the next review round |
| | *PROGRESS r5 (not cleared):* the queued findings have now been fixed and locked with mutation-verified tests — H-02, H-03, H-07, H-08, H-12, H-16, H-17, H-18, H-19, C2, C3, A1–A3, B1–B4, DT-01/02/03/05, M-05, M-06, R3-CP-07/08/09. This entry stays OPEN until an adversary re-attacks the fixes; a builder cannot clear a row that records its own unverified work | | | |

| L16 | **§10.3's Playwright e2e stage runs a minimal smoke, not substantive coverage — H20 RECORDED VARIANCE, approved 2026-08-16.** All five §10.3 stages now exist and run in CI (typecheck → unit → integration → invariants → Playwright e2e), plus the three gates. Phase 0 ships no endpoints and no client screens, so there is nothing for a real e2e suite to drive; the stage is installed and executing a smoke that proves it genuinely launches a browser. **The variance expires at Phase 1's gate, in the approver's words: no real e2e on the intake confirm flow, no Phase 1 pass.** That expiry is MECHANICAL, not remembered — `engine/test/invariants/invariants.test.ts` fails the build the moment PHASE reads 1 while the e2e suite is still smoke-only (adversary finding H-19) | Phase 0 deliverable (§10.3) | none for Phase 0 (variance approved and recorded); Phase 1 owns the expiry. **CORRECTION 2026-08-16 (adversary finding R5-02, R5-06):** this row overstated the enforcement. The expiry read the spec DIRECTORY and never the runner, so repointing `playwright.config.ts` at a stub left it "holding" with no browser launched; and it was satisfiable by a one-line string literal. **SECOND CORRECTION 2026-08-16 (adversary finding R6-06, R6-02, R6-03):** the first correction ALSO overstated it. "Requires a named test that drives a page" was two whole-file regexes ANDed — a named test with an empty body plus the string `"page."` loose in the file satisfied it, as did a commented-out real test; and `runnerTargets` took the FIRST `testDir` in the config, which an ordinary per-project override fooled with no intent to deceive. Both are now fixed: the matched test's own BODY must contain a `page.` call, an `await` and an `expect`, `.skip`/`.todo` do not count, and every `testDir` in the config must name the spec directory. Twice this row has claimed a guarantee stronger than the code delivered, in the artifact the human trusts most — treat any future strengthening claim here as unverified until an adversary has attacked it | Write real e2e coverage of the intake confirm flow before the Phase 1 gate; adversary confirms it fails on a seeded regression rather than passing vacuously, and that the smoke's own doc-comment is not what satisfies the check |

| L17 | **The spend meter's day is still an implementation detail of one implementation.** N-01's frozen-epoch default is fixed — the clock is a required constructor argument, so a meter with no day boundary is a compile error — but `SpendMeter.todayUsd(clientId)` still takes no date: the day is chosen by the meter's own clock rather than named by the caller, so a DO-backed meter written to this interface inherits that shape (adversary finding N-01, extending L14). **CORRECTED 2026-08-19 (adversary finding R12-05):** this row also said "rollover is UTC rather than client-local". That has been false since R7-02 — see L14's correction. The interface-shape half stands; the UTC half was a stale copy of L14's stale half, which is how one wrong sentence became two | Phase 0 (Law 2, §2.2) | H2 (Durable Object storage), plus the market registry's locale clock | Widen the interface to name the day explicitly; back it with DO storage keyed by client-local day; adversary restarts the DO mid-day and confirms the ceiling does NOT reset and that rollover happens on the client's midnight |
| L18 | **A Class-2 approval is bound to the target branch's TIP, so an unrelated merge into main invalidates a valid human signature.** This is the safe direction — it fails closed, and binding to the merge base instead let an attacker branch from an older commit, restore revoked content, and replay the approval that first authorized it, merging cleanly because the revert had restored the exact bytes (adversary findings N-03 leg C, N-10). The cost is that a human may have to re-sign. `npm run owed-approvals` makes that one command, but the friction is real and undisclosed until now | Phase 0 (§13) | none (design trade-off; H19 for the CODEOWNERS half) | Once CI runs on GitHub (L5), measure how often a re-sign is needed in practice; if it is frequent, bind to the merge base AND additionally require the from-content-hash to match the tip, so both replay and churn are covered |
| L19 | **One money-path guard is unguarded by mutation, and cannot honestly be made otherwise.** `getCaps` calls `assertCapsCoherent` on every lookup, but removing that call changes nothing observable, because every client in the frozen table IS coherent. A guard with no violating input in the repo cannot be killed by a mutation, and planting an incoherent client to make a test go red would mean shipping a bad cap table as test scaffolding. The CHECK itself is driven directly and its mutations are caught; only the call site is unproven. Recorded rather than faked | Phase 0 (Law 2, §10.1 step 4) | none (a stated limit of mutation testing, not a defect) | When a second real client is onboarded, or when caps become per-market, add a table-level validation run at import and confirm an incoherent entry is refused before the module finishes loading |
| L20 | **The H8 caps are signed but have never metered a real dollar.** The values are approved and enforced in code — the daily/monthly AI pair binds on every `llm()` call and is mutation-verified — but every run to date is against `MemorySpendMeter` in a sandbox. The ad-spend trio (pacing $66, hard $75, total $2,000) has NO enforcement path at all before Phase 6; it is recorded configuration, not a guard | Phase 0 (Law 2) → Phase 6 | H2 (Durable Object storage), H10 (Meta assets) | Drive the real meter against the live Gateway and confirm the monthly ceiling refuses at exactly $200; in Phase 6, adversary confirms the write adapter refuses at the $75 hard line and at $2,000 cumulative |

| L21 | **A reservation handle is now an identity, but only within one process.** `#close()` accepts only handles the meter minted, proved by a private WeakSet, which kills both the forged literal and the cross-instance replay r5 executed ($400 of real spend against a $200 ceiling, meter reading $200). **CORRECTED 2026-08-20 (adversary finding R13-07 leg D):** this row's stated mechanism named `#close()` and a handle WeakSet in the METER. Neither exists — identity now lives in the ledger's `#open` map, keyed by the handle object itself, and the meter has no `#close`. The row's CONCLUSION was still true, but a reader checking it would have found the wrong code, which is the stale-pointer defect L29 was corrected for twice. The conclusion is now bound by an executed claim in `engine/test/invariants/`: a forged copy of a real handle settles nothing and the real one settles once. The residual is the same ceiling L12 records for eval attestations: a WeakSet is in-process. A Durable-Object-backed meter that serialises handles across a restart cannot use one, and will need a signed or storage-resident nonce instead (adversary finding R5-01) | Phase 0 (Law 2, §2.2) | H2 (Durable Object storage) | Design the DO meter's handle authenticity BEFORE it stores anything; adversary attempts to settle and to release using a handle minted before a simulated eviction, and confirms both are refused |
| L22 | **Report and approval integrity now hold in CI, and nowhere else.** Reports and approvals are append-only, an unreadable report blocks, and the header must be pure prose — all enforced by `adversary-gate.mjs`, which runs only on a pull request. Nothing stops a direct push to a branch, and `APPROVALS/` is outside the tree scope so rewriting a signed approval does not move the hash a PASS is bound to. The gate catches it in the diff; branch protection is what makes the diff unavoidable (adversary findings R5-03, R5-05) | Phase 0 (§10.3, §13) | H19 | Enable branch protection with these gates required; adversary attempts a direct push that edits a signed approval and confirms it is refused |

| L23 | **Two money-path guards have no reachable input and are disclosed rather than faked.** `#read`'s corrupt-ledger check (`!Number.isSafeInteger(v) \|\| v < 0`) cannot fire: every value in those maps comes from arithmetic the `toMicros` range guard already bounds, and `#close` deletes an entry before decrementing so a negative cannot arise. `Object.hasOwn` on the caller-supplied narrowing table is inert with respect to money, because `narrow()` is `Math.min` and a polluted entry can only tighten. **CORRECTED 2026-08-20 (adversary findings R13-01 leg E, R13-07 leg D): this row was FALSE, and its stated reasons named deleted code.** `#read` and `#close` are both gone. More importantly the conclusion was wrong: `reserve` accepted a negative amount, `settle` committed it, the stored total went negative and the corrupt-ledger guard DID fire — bricking the client. What makes the row true again is R13-01's fix, not the reasoning it originally gave: `reserve` validates a positive whole number of micro-dollars at the boundary, so no contract call can store a value that is not money. That is now an executed claim in `engine/test/invariants/` — it drives six unusable amounts through the contract and asserts each is refused and the ledger stays readable — so the day the boundary check weakens, this row fails the build. Both guards remain fail-closed backstops against a future storage backend; neither carries a mutation entry, because one that always survives is noise (adversary finding R6-05, same class as L19) | Phase 0 (Law 2) | H2 (Durable Object storage) | When the meter is DO-backed, the ledger becomes externally writable and both guards become reachable — add entries then, and have the adversary corrupt stored state directly |
| L24 | **The e2e expiry checks that a browser is driven, not that the test is good.** At Phase 1 it requires a titled test whose body calls `page.`, awaits, and asserts. A test that navigates and asserts something trivial satisfies it. This is the honest ceiling of a static check; only a human or an adversary reading the test can judge whether the intake confirm flow is genuinely covered (adversary findings R6-02, R6-06) | Phase 1 gate (§10.3, H20) | none (design limit) | At the Phase 1 gate the adversary reads the e2e suite itself and judges coverage, rather than trusting the invariant that only counts syntax |

| L25 | **A third guard has no reachable input and is disclosed rather than faked.** `getCaps` validates each client's IANA zone on every lookup, but removing that call changes nothing observable because every client in the frozen table declares a resolvable one. The CHECK is driven directly and a bad zone is refused at `reserve()`; only the call site is unproven. Third member of the class L19 and L23 record (adversary finding R7-02) | Phase 0 (Law 2) | H2 (Durable Object storage), or a second real client | When a client is onboarded through a path that can supply a zone, the call site becomes reachable — add an entry then |
| L26 | **The metered cost is an ESTIMATE, trued up by daily reconciliation — not a measured provider charge.** Human ruling on adversary finding R7-05, 2026-08-16: the gateway meters the best-available number per call, and the data adversary reconciles against the provider's usage receipt daily, freezing scaling on drift beyond threshold — the same pattern as revenue reconciliation. Phase 0 is deliberately NOT gated on real-time provider cost, which the API may not expose. Until that reconciliation runs, the ledger's committed figure can differ from the real charge in either direction and nothing detects it | Phase 0 → Phase 2 (§3, Law 10) | H5 (Langfuse), H3 (ClickHouse for the reconciliation table) | Land the daily reconciliation job; adversary seeds a receipt whose actual cost exceeds the reservation and confirms the drift is detected, reported, and freezes scaling |
| L27 | **Class-2 approvals are UNVERIFIED until R7-07's lock is built.** Human ruling 2026-08-16: an approval proves bytes, not identity, and that is being closed now rather than deferred to H19. Branch protection plus CODEOWNERS on `APPROVALS/**` and `config/caps.ts` must require a PR approved by the owner's authenticated GitHub identity, with the approval commit pushed under their account and not agent-authored. **Every approval in `APPROVALS/` predating that lock — including the signed H8 caps entry — is to be treated as unverified**, and the two outstanding Class-2 sets are deliberately being held uncommitted until the new mechanism can verify them (adversary finding R7-07) | Phase 0 (§13, Law 15) | H19, now in scope rather than deferred | Enable protection; adversary authors a perfectly-formed approval as the automation principal and confirms the PR cannot merge |
| L28 | **`requireReservingMeter` is no longer a live guard on the `llm()` path.** A consequence of the R8-01 fix, found by the mutation harness rather than by a review: `llm()` now refuses any meter that is not a `FrozenCapsSpendMeter`, and every such meter inherits reserve/settle/release/reservedUsd from `MemorySpendMeter.prototype` — so nothing reaching that function can fail it, and dropping the `reservedUsd` requirement left the whole suite green where R5-07 had been caught for three rounds. The tests that used to reach it passed hand-built meter objects, which the brand refuses one line earlier. The contract is still real for any future implementation (`SpendMeter` declares all four optional so pre-F1 meters compile), so it is unit-tested directly rather than deleted — but its mutation entry is caught by that unit test, NOT by the money path, and this entry exists so the harness line is not read as protection it does not give | Phase 0 → whenever a second `SpendMeter` implementation ships (§3, F1/F2) | none — this is a disclosure, not a blocked verification | When a non-`MemorySpendMeter` meter is introduced, re-check whether the brand still admits only meters that satisfy the contract by construction; if not, the guard becomes live again and needs a money-path test |
| L29 | **The harness's own runner, its interrupt path and its drill have no mutation entries, by construction.** The table measures one thing: does a revert turn THE UNIT SUITE red. The runner loop, the signal path and `engine/test/drill/` are none of them executed by that suite — the drill deliberately so since R10-05, because inside the suite it spawned a harness per suite run, mutated the money-path file and orphaned a worker tree each time. Entries written for these SURVIVE because the suite never runs them, not because anything is unprotected, and one (adding the drill to `vitest.config.ts`'s include) makes the suite spawn harnesses recursively. They are covered instead by `npm run drill` as its own CI stage, and by the invariant that reads the runner config and asserts the drill is excluded. Originally recorded for the interrupt `await` alone (R9-03); widened after R10-07b. A SIGINT must stop a run and restore the tree (adversary finding R9-03), and that is proven by executing it: the drill in `engine/test/integration/gate-cli.test.ts` spawns the real harness, waits until it has genuinely broken a file, signals it, and asserts the file came back. **CORRECTED 2026-08-20 (adversary finding R14-10): the reason stated here describes behaviour R10-06 REMOVED, and this is the third false claim in this row.** The drill does not decline on a live marker — R10-06 found that the decline was itself the defect (`echo '{"pid":1}' > .mutate-inflight.json` permanently disabled the only behavioural proof while the drill reported PASS), and it now FAILS on a pre-existing marker. The true reason an entry on the drill always survives is simpler and was true all along: `vitest.config.ts` does not match `*.drill.ts`, so `npm test` never runs the file the harness measures. What changed this round is that the drill's DECISION no longer lives in the un-run file: `postSignalWrites` is a pure function in the default suite with its own red-proof, after all three of the drill's inline detection paths were deleted and it still reported PASS (adversary finding R14-06). An entry that cannot fail still reads as coverage, so there is still none on the drill file itself — but the thing it decides is now tested. **CORRECTION 2026-08-19 (human ruling, R11 round):** two of this row's claims were false when written and are corrected here rather than quietly restated. (a) *"The harness's own runner ... ha[s] no mutation entries, by construction"* is wrong. `mutate.mjs` carries live entries today — the crash-marker-before-source-break ordering, the entry-point guard, the recovery call, and the blocking-call binding — and every one of them is CAUGHT. They are caught not by executing the runner but by `engine/test/invariants/`, which reads the runner and drives its structural properties, and which the unit suite DOES run. What genuinely has no entry is the SIGNAL PATH (a delivered SIGINT restoring the tree) and the drill file itself. (b) The drill has not lived in `engine/test/integration/gate-cli.test.ts` since R10-05 moved it out; it is `engine/test/drill/harness-interrupt.drill.ts`, run by `npm run drill` under `vitest.drill.config.ts` as its own CI stage. The row named a file that no longer contains it, which is precisely the kind of stale pointer that lets a disclosure outlive its facts. **SECOND CORRECTION 2026-08-19 (adversary finding R12-05): the first correction was itself wrong in two particulars, which is the third consecutive round in which a correction introduced a fresh false claim.** (i) `mutate.mjs` carries exactly THREE entries — the crash-marker ordering, the entry-point guard, and the recovery call. The blocking-call-binding entries do NOT target `mutate.mjs`; they target `engine/test/blocking-calls.ts`. (ii) Those entries are caught by `engine/test/blocking-calls.test.ts`, the resolver's own red-proof, NOT by `engine/test/invariants/`. Under the standing rule adopted this round — a row asserting something about code behaviour carries a test that fails when the assertion goes stale — the counts and targets in this row are bound by the ledger-claims check in `engine/test/invariants/`, so a fourth wrong correction fails the build instead of waiting for the next adversary | Phase 0 → whenever the marker path stops being fixed (§10.3) | none — this is a disclosure, not a blocked verification | If the marker ever becomes per-run (a pid or temp path), the drill can run nested and this behaviour should get a real entry |
| L30 | **Five clock-family guards are dead ON THE PRODUCTION PATH, kept for the test-only meter, and this records which.** R9-05 bound `FrozenCapsSpendMeter` to a clock it constructs itself, so the guards that exist because a caller could supply a clock — the `typeof now !== "function"` constructor check, `zoneDayKey`'s non-finite refusal, `#assertForward`'s backwards-clock refusal and its advancing half, and the one-clock-read pairing in `#periods` — can no longer be reached through `llm()`. They remain reachable on `MemorySpendMeter`, which the tests drive directly and which `llm()` refuses, so every one keeps a real contract and a live mutation entry; what changed is WHOSE path they guard (adversary finding R10-07c). **CORRECTION 2026-08-19 (human ruling, R11 round):** this row said the production meter binds `Date.now`. It has not since `d73df4c` (R10-03): it binds `trustedClock()`, which cross-validates three independent wall-clock sources at construction and then advances on `process.hrtime.bigint()` deltas, precisely so that a pre-import patch of `Date.now` cannot move the accounting day. The correction matters because the row's *reason* — that the clock is no longer caller-supplied — is unchanged, but its *mechanism* was a full round out of date, and a reader checking the claim would have found the wrong code. Additionally, the unreachable-guard sweep no longer records `something threw`: since R11-02's fix it identifies WHICH guard refused. **SECOND CORRECTION 2026-08-19 (adversary findings R12-02, R12-05):** the sentence that stood here — "these five are now verified to fire on their own messages" — was false for two of the five, and was written in the very commit that corrected this row for carrying a false claim. The sweep covers THROWING guards, and two of the five are not throws: the high-water ADVANCE and the one-clock-read pairing in `#periods` are control flow, so neither can "fire on its own message" and neither was ever in the sweep. They are covered by named mutation entries instead — real coverage, but a different instrument, and the row has to say which. The sweep's population is now read out of the source rather than hand-written (R12-02), so this row's THROWING members are covered by construction and a new one cannot slip out. Also extends L19/L25: `getCaps`'s five `assertSaneCap` calls are dead for the same reason those entries record — every client in the frozen table is sane — while `narrow`'s `assertSaneCap` on the caller-supplied table stays live and is caught | Phase 0 → whenever a second production meter or a non-frozen caps source ships (§3) | none — this is a disclosure, not a blocked verification | When a meter other than `FrozenCapsSpendMeter` reaches `llm()`, re-check each of these five: they become live guards again and need money-path tests, not just unit tests on the test-only meter |
| L31 | **THE IN-PROCESS LEDGER IS ADVISORY. THE AUTHORITATIVE CONTROL IS OUT OF PROCESS (L4).** Human ruling 2026-08-21 on adversary finding R14-01, which measured what four rounds of hardening had not: an in-process ledger cannot bound its own process. A prototype patch attacks the CALL, not the state, so a store that is never called cannot refuse — $30 through a $5/day ceiling with a DO-shaped store's counters at zero. Prototype mutability is a property of JavaScript. The ruling accepts the residual as irreducible and stops the hardening. **The layer split:** PRIMARY is the AI Gateway's per-client cap (L4), enforced on the far side of `transport.post` where nothing in this process can reach it. ADVISORY is `spend-ledger.ts`, which refuses a breach before an upstream call is made — worth keeping for the money and the round trip it saves, never worth describing as the authority. **Phase 2's design goal changes with it:** not "a ledger that cannot be bypassed" but "correct when the in-process ledger is absent, patched, or never called". The Durable Object still closes the PER-PROCESS bound (two Workers, two ceilings) and that is now the whole of what it is for. **PROVEN BY EXECUTION, per the ruling's §4** `[VERIFIED engine/test/gateway-cap-primary.test.ts]`: the disclosed attack is run — `reserve` and `settle` neutered on the prototype the live ledger resolves through — and the out-of-process cap still binds, the refusal reaches the caller and is never swallowed or returned as output, and this ledger records $0.00 throughout. Verified red by making `llm()` swallow a transport rejection. The advisory layer's own value is measured too: with both live, the Gateway is never asked past the ledger's ceiling. **WHAT REMAINS OPEN, and it is the reason Phase 0 cannot close on this row:** **(a)** `[LIMITATION]` the test proves the ENGINE is correct against a Gateway that refuses. It cannot prove the real Gateway is CONFIGURED with these ceilings — that is L4, blocked on H2. Until L4 clears, the primary control is designed and unprovisioned, and no in-process test can substitute for it. **(b)** `[LIMITATION]` the ledger is per PROCESS; two Workers hold two advisory ledgers. Closed by the Durable Object. **(c)** `[LIMITATION]` the process slot is a capability the process boundary exposes. `slot()` refuses an occupant it did not mark and the slot is non-writable and non-configurable — those are removals — but a registry-marked impostor planted before first use is indistinguishable in-process. `[VERIFIED engine/test/ledger-slot.test.ts]` **(d)** `[LIMITATION]` an in-process prototype patch spends unmetered, on `MemorySpendMeter.prototype` AND `InMemorySpendLedger.prototype`. Accepted as irreducible by the ruling above; bounded and measured rather than described. `[VERIFIED engine/test/locks-r12.test.ts]` | Phase 0 → Phase 2 (§2.2, Law 2) | **H2 — and this is now a Phase 0 blocker, not a Phase 2 one**, because the primary control is Gateway-side | Configure the Gateway caps to match `caps.ts`; adversary drives spend with the in-process ledger neutered against the REAL Gateway and confirms it refuses at the frozen ceiling; then across two Worker instances; then confirms a restart does not reset it |
| L32 | **`R7-04 departed set before dispatch` reports CAUGHT from a MOCKED path, and that is disclosed rather than dressed up.** The only test that goes red when `departed = true` is moved after the dispatch is `engine/test/departed-contract.test.ts`, which `vi.mock`s `engine/src/spend-meter.ts` to drive a deliberately non-conforming meter. So the harness line is evidence about the CONTRACT, not about production (adversary finding R11-05; its identifier was then reassigned to R11-02's fix, so for one round the record read as though it were closed — R12-09). WHY NO UNMOCKED TEST EXISTS, measured rather than assumed: on the money path `departed` only changes an outcome when `settle` fails and `release` still works. Against `InMemorySpendLedger` that pair cannot occur — both refuse on the same unavailability check and both succeed otherwise — so there is no input to production that discriminates. The guard exists for a `SpendLedger` implementation where they CAN diverge, which is precisely Phase 2's Durable Object: a settle that fails on a network blip while a local release succeeds. Human ruling 2026-08-18 kept the guard for exactly that reason. WHAT IS BOUNDED NOW: `engine/test/invariants/` enumerates every `vi.mock` of a module under `*/src/` across the whole test tree and fails on any that is not declared with a ledger row — so a second mocked money-path file cannot appear unnoticed, and a declaration that goes stale fails too. This row is the declaration for the one that exists. | Phase 0 → Phase 2 (§2.2) | H2 (Durable Object storage) | When the DO-backed ledger lands, drive a settle that fails while release succeeds and confirm `departed` keeps the headroom held — then this entry becomes an unmocked money-path test and the row clears |
| L33 | **Resuming a halted client is NARROWED, not controlled.** `setAvailable(c, true, reason)` cleared an operator's halt with no authority check, so the module whose runaway spend caused the halt could lift it (adversary finding R14-08). A resume must now quote the halt reason it lifts, which stops the accidental and the careless case and is driven in the sweep. It is explicitly NOT authority: any in-process caller that can read `availabilityAudit()` can read the reason back and quote it. Real authority needs an operator identity this process does not have. Stated as a narrowing rather than a closure, per the standing rule of 2026-08-20. | Phase 0 → Phase 2 (§2.2) | H2 (Durable Object storage), and an operator identity for halt/resume | When the halt lives in the Durable Object, adversary attempts a resume from a Worker with no operator credential and confirms it is refused; and confirms the audit records who resumed, not only why |
| L34 | **Seven verdicts were being reached where the default suite could not see them, and the rule that forbids it had been applied to exactly one file.** R14-06 established that a checker running under its own runner is unprovable by `npm test` and must have its decision extracted as a pure function with red-proofs there. It was then applied to the SIGINT drill and to nothing else. Auditing every runner — the five `.mjs` CLIs and both drills (HANDOFF §7.2) — found seven decisions living inside a runner, and each was **measured** surviving a one-line revert with the whole default suite green at 354/354 before it was touched: the leak scan's CLI verdict wiring, its file-extension allowlist and its skipped-directory set; the adversary gate's phase-report selection and its verified-tree scope (`.github/` removable in one line, restoring R2-18); the class-2 gate's approval-document selection (a rewritten approval could authorize a new transition); and the mutation harness's own CAUGHT/SURVIVED classification — a SECOND copy of the comparison the meta-check validates, so inverting it made every survivor print CAUGHT and the run exit 0 while the meta-check passed. **CAPABILITIES REMOVED:** no runner decides what to read, which report answers for a phase, which document is an approval, what the verified tree covers, or whether a mutation was caught, from inside a process the default suite never starts. All seven live in `gate-lib.mjs`, `scan-lib.mjs` or `mutate-lib.mjs`, each with unit red-proofs, and the CLI wiring that consumes them is executed end-to-end `[VERIFIED engine/test/integration/leak-cli.test.ts, engine/test/integration/gate-cli.test.ts]`. Driving the extension decision against the tracked tree then found a real coverage hole: `haven/terraform/aws/variables.tf`, `haven/Dockerfile.dev`, a Prisma schema, CSS, `.nvmrc` and four `.gitignore` files had never been read by the leak scan. The allowlist is now a denylist of binary types with a NUL-byte measurement behind it, and the test derives its population from `git ls-files` and lets the BYTES adjudicate, so a new file type fails the day it lands. **WHAT KEEPS IT TRUE:** the runner-decision sweep in `engine/test/invariants/` enumerates runners from the filesystem (npm scripts + the CI workflow + `engine/test/drill/`), and fails if any is unbound, if a declared prover is not in the default suite's include globs, if a decision module is not actually imported at both ends, or if it carries no mutation entry — which is how `diff-lib.mjs`, the git-diff parser both gates depend on, was found to have had none. **`[LIMITATION]` the sweep catches decision LITERALS in a runner, not every undelegated decision.** A comparison written inline in a runner's control flow is still invisible to it — the harness classification, the seventh survivor, was exactly that shape and was found by measurement, not by the sweep. Finding those takes a mutate-and-run round. Said plainly rather than left to read as full coverage | Phase 0 (§10.3) | none — this is done, not blocked | Each new runner: confirm the sweep fails before its binding is added, and that its decision module's mutation entries are CAUGHT |
| L35 | **The leak scan and the adversary's verdict cover DIFFERENT TREES, deliberately, and until now that was nowhere stated.** The verified-tree hash a PASS binds to is `fullburn/` + `.github/` minus `reports/` and `APPROVALS/` (`VERIFIED_TREE_SCOPE`). The leak scan walks the WHOLE repository, because a leaked token is a leak wherever it sits (adversary findings R2-29, H-16). Both are correct and they are not the same statement, so a reader of a PASS report cannot tell from the report which tree the leak verdict ran over. **The concrete consequence:** this repository holds two sibling product trees — `haven/` (243 tracked files) and `pulsern/` (125) — that are SCANNED FOR SECRETS ON EVERY PUSH but are OUTSIDE the adversary's verified tree, so they can change arbitrarily under a standing fullburn PASS without invalidating its binding. That is the intended behaviour: they are not fullburn's code and a fullburn verdict should assert nothing about them. It is recorded because an unstated asymmetry is the shape a stale claim grows in, and because the r14 audit widened the scan's population by 21 files across all three trees without anyone writing down that the two scopes had ever differed. **`[VERIFIED engine/test/invariants/invariants.test.ts — the L35 claim]`** which DERIVES both sets from git: every top-level tracked directory outside `VERIFIED_TREE_SCOPE` must be visited by the leak-scan walk, `haven` and `pulsern` must not be inside the verified scope, and the sibling set must be exactly those two — so a THIRD product tree added tomorrow fails this row rather than silently joining the unverified set. **`[LIMITATION]`** the row asserts scope, not sufficiency: it says the sibling trees are read, not that the secret rules would match every credential format in them. See the open rule-coverage finding in `RUNNER_AUDIT_2026-08-21.md` §6b — `.npmrc`, `.netrc`, `.pgpass` and `PGPASSWORD=` are read and would not be matched | Phase 0 (§10.2, §15) | none — this is a disclosure, not a blocked verification | If a sibling tree is ever brought inside the verified scope, or split out of the repository, update this row and its claim together; a split is decided and deferred as of 2026-08-22 |

Ledger is append-only: cleared entries get a `CLEARED <date> <evidence>` line,
never deletion.
