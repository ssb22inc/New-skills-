{
  "name": "fullburn",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "config",
    "engine"
  ],
  "scripts": {
    "typecheck": "tsc --build",
    "test": "vitest run",
    "leak-check": "node engine/scripts/leak-check.mjs",
    "invariants": "vitest run --dir engine/test/invariants"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
